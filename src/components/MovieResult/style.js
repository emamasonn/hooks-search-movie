import { makeStyles } from '@material-ui/styles';

export default makeStyles({
    cardContainer:{
        marginBottom: 8,
    },
    poster: {
        width: 120,
    },
    titleContainer: {
        paddingLeft: 8 
    }
})